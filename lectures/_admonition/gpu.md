```{admonition} GPU
:class: warning

This lecture was built using a GPU.

To run this lecture on a [Google Colab](https://colab.research.google.com/) machine, click "play" (top right), select Colab, and change the runtime environment to include a GPU.

To run this lecture on your own machine (via CPU or GPU), install [Google JAX](https://github.com/google/jax).
```
